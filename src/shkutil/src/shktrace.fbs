namespace ShkTrace;

table Input {
  path:string (required);
  directory_listing:bool;
}

table Trace {
  inputs:[Input];
  outputs:[string];
  errors:[string];
}

file_extension "trace";
root_type Trace;
