#pragma once

namespace shk {

class FileSystem {
  virtual ~FileSystem() = default;

  // FIXME(peck): Add stuff here
  // * lstat
  // * open
  // * fread
  // * fwrite
  // * fclose
  // * ftell
  // * feof
  // * ferror
};

}  // namespace shk
